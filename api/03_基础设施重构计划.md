# 阶段 3: 核心基础设施重构计划

> **目标**: 建立配置管理、异常处理、密码处理基础设施
> **时间**: 1.5 小时
> **依赖**: 阶段 0

---

## 🎯 重构目标

- [ ] 创建配置管理模块（api/config/）
- [ ] 创建核心功能模块（api/core/）
- [ ] 统一异常处理
- [ ] 统一密码处理

---

## 📋 任务清单

### 任务 1: 创建配置管理

**新建目录结构**:
```
api/
└── config/
    ├── __init__.py
    ├── app_config.py
    └── constants.py
```

**新建**: `api/config/constants.py`

```python
"""
常量定义
将硬编码值集中管理
"""

# ===== 内容状态 =====
CONTENT_STATUS_DRAFT = 'draft'
CONTENT_STATUS_PENDING = 'pending'
CONTENT_STATUS_REVIEWED = 'reviewed'
CONTENT_STATUS_REJECTED = 'rejected'
CONTENT_STATUS_PUBLISHED = 'published'
CONTENT_STATUS_TERMINATED = 'terminated'

ALLOWED_CONTENT_STATUSES = [
    CONTENT_STATUS_DRAFT,
    CONTENT_STATUS_PENDING,
    CONTENT_STATUS_REVIEWED,
    CONTENT_STATUS_REJECTED,
    CONTENT_STATUS_PUBLISHED,
]

# ===== Typst 配置 =====
TYPST_VERSIONS = {
    'no': 1,
    'first-v': 3,
    'lecture-v': 3,
    'other-v': 3,
    'college-v': 3,
    'club-v': 3,
}

TYPST_CATEGORIES = ['college', 'club', 'lecture', 'other']

# ===== 文件上传 =====
ALLOWED_IMAGE_EXTENSIONS = ['.jpg', '.jpeg', '.png', '.gif', '.webp']
MAX_FILE_SIZE = 10 * 1024 * 1024  # 10MB
```

**新建**: `api/config/app_config.py`

```python
"""
应用配置类
"""

class AppConfig:
    """应用配置类"""

    # 密码策略
    PASSWORD_MIN_LENGTH = 8

    # 搜索配置
    SEARCH_MIN_LENGTH = 1
    SEARCH_MAX_RESULTS = 100

app_config = AppConfig()
```

### 任务 2: 创建核心功能模块

**新建目录结构**:
```
api/
└── core/
    ├── __init__.py
    ├── exceptions.py
    └── password_handler.py
```

**新建**: `api/core/exceptions.py`

```python
"""
自定义异常类
"""

class APIException(Exception):
    """API 基础异常"""
    def __init__(self, message, code='error', status=400):
        self.message = message
        self.code = code
        self.status = status
        super().__init__(message)

class ValidationError(APIException):
    """数据验证错误"""
    def __init__(self, message):
        super().__init__(message, code='validation_error', status=400)

class AuthenticationError(APIException):
    """认证错误"""
    def __init__(self, message):
        super().__init__(message, code='authentication_error', status=401)

class PermissionDeniedError(APIException):
    """权限错误"""
    def __init__(self, message):
        super().__init__(message, code='permission_denied', status=403)

class NotFoundError(APIException):
    """资源不存在"""
    def __init__(self, message):
        super().__init__(message, code='not_found', status=404)

class BusinessLogicError(APIException):
    """业务逻辑错误"""
    def __init__(self, message):
        super().__init__(message, code='business_logic_error', status=422)
```

**新建**: `api/core/password_handler.py`

```python
"""
密码处理器
封装 MD5 逻辑（保持与现有系统兼容）
"""

import hashlib


class PasswordHandler:
    """密码处理器"""

    ALGORITHM = 'md5'  # 保持 MD5（与现有系统兼容）

    @staticmethod
    def hash_password(password: str) -> str:
        """哈希密码"""
        return hashlib.md5(password.encode('utf-8')).hexdigest()

    @staticmethod
    def verify_password(password: str, hashed: str) -> bool:
        """验证密码"""
        input_hash = hashlib.md5(password.encode('utf-8')).hexdigest()
        return input_hash == hashed
```

---

## ✅ 验证标准

- [ ] 所有配置文件创建完成
- [ ] 所有异常类创建完成
- [ ] 密码处理器创建完成
- [ ] 运行 `python test_api.py` 全部通过
- [ ] 认证功能正常（登录/注册/修改密码）

---

## 📝 执行步骤

1. **创建目录结构**
2. **创建配置文件**
3. **创建核心功能模块**
4. **运行测试验证**

---

*阶段 2 计划由 Claude Code 生成*
*最后更新: 2026-02-15*
