# Django API 协调重构计划（总览）

> **目标**: 引入 Service 层，分离业务逻辑与视图层，提升代码可维护性和可测试性
> **原则**: 不修改数据库字段，不使用 migrate，只做代码层面的重构
> **日期**: 2026-02-15
> **范围**: django_models/ (233行) + api/ (2508行)

---

## 📚 文档结构

```
api/
├── 重构计划.md                  # 本文件 - 总览和导航
├── 01_测试基准计划.md            # 阶段 0：测试基准
├── 02_django_models重构计划.md   # 阶段 1：模型层重构
├── 03_基础设施重构计划.md         # 阶段 2：配置+异常+密码
├── 04_服务层设计.md              # 阶段 3：服务层详细设计
├── 05_视图层重构计划.md            # 阶段 4-10：各个视图模块
└── 执行检查清单.md               # 执行跟踪和验证
```

---

## 🎯 重构目标

### 代码质量提升

| 指标 | 当前 | 目标 | 改进幅度 |
|------|------|------|----------|
| views 总行数 | 1690 行 | ~750 行 | ↓ 56% |
| models 总行数 | 233 行 | ~150 行 | ↓ 35% |
| 最长文件 | 609 行 | 250 行 | ↓ 59% |
| 业务逻辑位置 | 视图层 | 服务层 | ✅ 分离 |
| 查询逻辑位置 | 视图层 | Manager 层 | ✅ 分离 |
| 可测试性 | 低 | 高 | ✅ 提升 |

### 架构改进

```
重构前                        重构后
视图层 (1690行)      →      视图层 (~750行)
↓                             ↓
业务逻辑耦合                  服务层 (~800行)
↓                             ↓
模型层 (233行)       →      模型层 (~150行)
                              ↓
查询层（无）                   Manager层 (~100行)
```

---

## 📋 重构阶段概览

| 阶段 | 名称 | 时间 | 详细文档 |
|------|------|------|----------|
| 0 | 测试基准建立 | 3h | `01_测试基准计划.md` |
| 1 | django_models/ 基础重构 | 1h | `02_django_models重构计划.md` |
| 2 | 核心基础设施 | 1.5h | `03_基础设施重构计划.md` |
| 3 | 创建服务层 | 6h | `04_服务层设计.md` |
| 4 | 认证模块重构 | 1.5h | `05_视图层重构计划.md#认证模块` |
| 5 | 内容管理重构 | 2h | `05_视图层重构计划.md#内容管理` |
| 6 | 发布管理重构 | 2.5h | `05_视图层重构计划.md#发布管理` |
| 7 | 用户管理重构 | 1.5h | `05_视图层重构计划.md#用户管理` |
| 8 | 文件处理重构 | 1h | `05_视图层重构计划.md#文件处理` |
| 9 | 视图层简化 | 2h | `05_视图层重构计划.md#全面简化` |
| 10 | 文档测试完善 | 2.5h | `执行检查清单.md` |

**总计**: 24.5 小时（约 3-4 周）

---

## ⚠️ 重要约束

### 🗄️ 数据库约束（最高优先级 - 必须遵守）

> **原则**: SQL 数据库结构是唯一真理来源（Single Source of Truth）

#### 禁止操作
- ❌ 不能修改数据库表结构
- ❌ 不能使用 Django migrate
- ❌ 不能修改任何字段定义
- ❌ 不能添加新字段到数据库
- ❌ 不能删除数据库字段

#### 必须遵守
- ✅ **所有 models.py 必须与 SQL 保持 100% 一致**
- ✅ 字段名、类型、NULL 约束必须与数据库完全相同
- ✅ 发现不一致时，以 SQL 为准修改 models.py
- ✅ 参考本文档"数据库规范"章节进行验证

#### 已知字段不一致问题（必须在阶段 1 修复）
1. **Content 模型**: 缺失 `locker_id` 和 `locked_at` 字段
2. **Comment 模型**: 字段名错误（`created_at`/`updated_at` 应为 `created`/`updated`）

---

### 🔄 向后兼容性（必须保证）
- ✅ 模型的 @property 兼容层保持旧接口可用
- ✅ 默认 Manager (objects) 保持不变
- ✅ 所有 API 端点保持兼容
- ✅ 前端无需修改即可正常工作

---

## 🗄️ 数据库规范（SQL Schema）

> **重要**: 所有重构必须严格遵守本规范，models.py 定义必须与数据库表结构一致

### 数据库基本信息

```
数据库名: seu_news
字符集: utf8mb4
排序规则: utf8mb4_0900_ai_ci
引擎: InnoDB
```

### 表 1: user_info（用户信息表）

| 字段名 | 类型 | NULL | 键 | 默认值 | 说明 |
|--------|------|------|-----|--------|------|
| id | INT | NO | PRI | AUTO_INCREMENT | 唯一主键 |
| username | VARCHAR(30) | NO | UNI | NULL | 用户名（唯一） |
| password_MD5 | VARCHAR(100) | NO | | NULL | 密码 MD5 哈希值 |
| avatar | VARCHAR(100) | YES | | NULL | 用户头像图片名 |
| realname | VARCHAR(30) | NO | MUL | NULL | 真实姓名 |
| student_id | VARCHAR(30) | NO | MUL | NULL | 学号 |
| role | INT UNSIGNED | NO | | NULL | 权限位（0:用户 1:编辑 2:管理员 3:超管） |
| created_at | DATETIME | NO | MUL | CURRENT_TIMESTAMP | 账号创建时间 |
| updated_at | DATETIME | NO | MUL | CURRENT_TIMESTAMP | 最后更新时间 |
| last_login | DATETIME | YES | | NULL | 最后登录时间 |

**索引**:
- PRIMARY KEY (`id`)
- UNIQUE KEY `idx_username` (`username`)
- KEY `idx_realname` (`realname`)
- KEY `idx_student_id` (`student_id`)
- KEY `idx_created_at` (`created_at`)
- KEY `idx_updated_at` (`updated_at`)

**权限系统说明**:
- `0 (0b00)`: 普通用户 - 只读权限
- `1 (0b01)`: 编辑 - 可创建/编辑/删除内容
- `2 (0b10)`: 管理员 - 可管理用户、DDL
- `3 (0b11)`: 超级管理员 - 兼具编辑和管理员权限

---

### 表 2: content_management（内容管理表）

| 字段名 | 类型 | NULL | 键 | 默认值 | 说明 |
|--------|------|------|-----|--------|------|
| id | INT | NO | PRI | AUTO_INCREMENT | 唯一主键 |
| creator_id | INT | NO | MUL | NULL | 创建者用户ID |
| describer_id | INT | NO | MUL | NULL | 描述者用户ID |
| reviewer_id | INT | YES | MUL | NULL | 审核者用户ID |
| title | VARCHAR(200) | NO | | NULL | 内容标题 |
| short_title | VARCHAR(100) | YES | | NULL | 短标题（用于展示） |
| link | TEXT | NO | | NULL | 内容链接地址 |
| content | TEXT | NO | | NULL | 详细内容 |
| deadline | DATETIME | YES | MUL | NULL | 截止时间（DDL） |
| image_list | TEXT | NO | | '[]' | 图片列表（JSON数组） |
| publish_at | DATETIME | YES | MUL | NULL | 实际发布时间 |
| type | VARCHAR(50) | NO | MUL | NULL | 内容类型（教务/竞赛/活动等） |
| tag | TEXT | YES | | NULL | 内容标签（自由文本） |
| status | VARCHAR(50) | NO | MUL | 'draft' | 内容状态 |
| **locker_id** | **INT** | **YES** | | **NULL** | **当前锁定者用户ID（防止并发编辑）** |
| **locked_at** | **DATETIME** | **YES** | | **NULL** | **锁定时间戳** |
| created_at | DATETIME | NO | MUL | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | NO | MUL | CURRENT_TIMESTAMP | 最后更新时间 |

**索引**:
- PRIMARY KEY (`id`)
- KEY `idx_creator_id` (`creator_id`)
- KEY `idx_describer_id` (`describer_id`)
- KEY `idx_reviewer_id` (`reviewer_id`)
- KEY `idx_status` (`status`)
- KEY `idx_type` (`type`)
- KEY `idx_deadline` (`deadline`)
- KEY `idx_publish_at` (`publish_at`)
- KEY `idx_created_at` (`created_at`)
- KEY `idx_updated_at` (`updated_at`)

**状态流转说明**:
- `draft` → `pending` → `reviewed` → `published` （正常流程）
- `reviewed` → `rejected` （审核拒绝）
- `draft` → `terminated` （直接终止）
- `published` → 可以回退到 `draft` （撤回已发布）

**三人协作机制**:
- `creator_id`: 内容创建者
- `describer_id`: 内容描述者（补充详情）
- `reviewer_id`: 内容审核者（批准/拒绝）

**⚠️ 重要**: `locker_id` 和 `locked_at` 字段在数据库中存在，但当前 models.py 中缺失，需要在阶段 1 补充

---

### 表 3: comment_management（评论管理表）

| 字段名 | 类型 | NULL | 键 | 默认值 | 说明 |
|--------|------|------|-----|--------|------|
| id | INT | NO | PRI | AUTO_INCREMENT | 唯一主键 |
| comment | TEXT | NO | | NULL | 评论内容 |
| creator_id | INT | NO | MUL | NULL | 评论创建者用户ID |
| news_id | INT | NO | MUL | NULL | 关联新闻ID |
| parent_comment_id | INT | YES | MUL | NULL | 回复的上级评论ID（NULL=顶层评论） |
| **created** | **DATETIME** | **NO** | **MUL** | **CURRENT_TIMESTAMP** | **创建时间** |
| **updated** | **DATETIME** | **NO** | **MUL** | **CURRENT_TIMESTAMP** | **最后更新时间** |

**索引**:
- PRIMARY KEY (`id`)
- KEY `idx_creator_id` (`creator_id`)
- KEY `idx_news_id` (`news_id`)
- KEY `idx_parent_comment_id` (`parent_comment_id`)
- KEY `idx_created` (`created`)
- KEY `idx_updated` (`updated`)

**⚠️ 重要**: 数据库字段名是 `created` 和 `updated`，但当前 models.py 中是 `created_at` 和 `updated_at`，需要在阶段 1 修复

---

### 表 4: django_session（Django 会话表）

| 字段名 | 类型 | NULL | 键 | 默认值 | 说明 |
|--------|------|------|-----|--------|------|
| session_key | VARCHAR(40) | NO | PRI | NULL | Session 密钥（唯一标识） |
| session_data | LONGTEXT | NO | | NULL | Session 数据（JSON 格式编码） |
| expire_date | DATETIME(6) | NO | MUL | NULL | Session 过期时间（精确到微秒） |

**索引**:
- PRIMARY KEY (`session_key`)
- KEY `django_session_expire_date_idx` (`expire_date`)

---

### 🔍 字段一致性问题（必须在阶段 1 修复）

| 问题类型 | 表名 | 数据库字段名 | models.py 字段名 | 修复方案 |
|---------|------|-------------|-----------------|----------|
| 缺失字段 | content_management | `locker_id`, `locked_at` | ❌ 不存在 | 添加到 models.py |
| 字段名不一致 | comment_management | `created`, `updated` | ❌ `created_at`, `updated_at` | 修改为 `created`, `updated` |

---

### 📖 参考文档

完整的 SQL 建表脚本: `D:\pythonProj\seu-news\create_tables.sql`

**验证命令**:
```sql
-- 查看所有表
SHOW TABLES;

-- 查看表结构
DESCRIBE user_info;
DESCRIBE content_management;
DESCRIBE comment_management;
DESCRIBE django_session;

-- 查看索引
SHOW INDEX FROM user_info;
SHOW INDEX FROM content_management;
```

---

## 🚀 快速开始

### 推荐执行顺序

1. **阅读本文档** - 了解整体架构和数据库规范 ⚠️ **必须先阅读"数据库规范"章节**
2. **验证 SQL 规范** - 确认数据库表结构与文档一致
   ```bash
   # 连接数据库并验证
   mysql -u root -p seu_news
   DESCRIBE user_info;
   DESCRIBE content_management;
   DESCRIBE comment_management;
   ```
3. **阅读 `01_测试基准计划.md`** - 建立测试基础
4. **按顺序执行各阶段** - 每个阶段独立且可验证

### 每个阶段的工作流程

```bash
# 1. 开始前
python test_api.py  # 确保当前状态正常
cp -r api api_backup_stage_N  # 备份

# 2. 执行重构
# 按照详细文档执行
# ⚠️ 时刻参考"数据库规范"章节，确保 models.py 与 SQL 一致

# 3. 完成后
python test_api.py  # 确保功能正常
```

### ⚠️ 关键检查点

在开始任何重构前，请确认：

- [ ] 已阅读并理解"数据库规范"章节
- [ ] 已验证数据库表结构与文档一致
- [ ] 已了解字段不一致问题（Content 的 locker_id/locked_at，Comment 的 created/updated）
- [ ] 已备份当前代码

---

## 📊 进度跟踪

当前状态: **✅ 所有阶段已完成**

| 阶段 | 状态 | 完成时间 | 备注 |
|------|------|----------|------|
| 0 | ✅ 已完成 | 2026-02-15 | 测试基准建立完成 |
| 1 | ✅ 已完成 | 2026-02-15 | django_models 重构完成 |
| 2 | ✅ 已完成 | 2026-02-15 | 核心基础设施重构完成 |
| 3 | ✅ 已完成 | 2026-02-15 | 服务层创建完成（关键里程碑） |
| 4 | ✅ 已完成 | 2026-02-15 | （已包含在阶段 3 中） |
| 5 | ✅ 已完成 | 2026-02-15 | 认证和内容模块完成 |
| 6 | ✅ 已完成 | 2026-02-15 | 发布管理重构完成 |
| 7 | ✅ 已完成 | 2026-02-15 | 用户管理重构完成 |
| 8 | ✅ 已完成 | 2026-02-15 | 文件处理重构完成 |
| 9 | ✅ 已完成 | 2026-02-15 | 全面优化完成 |
| 10 | ✅ 已完成 | 2026-02-15 | 文档测试完善 |

**重构进度**: 5/5 视图模块完成 (100%)
**整体项目完成度**: 100% ✅

---

## 📖 相关文档

### 设计模式
- [Service Layer Pattern](https://martinfowler.com/eaaCatalog/serviceLayer.html)
- [Repository Pattern](https://martinfowler.com/eaaCatalog/repository.html)

### Django 最佳实践
- [Django Best Practices](https://docs.djangoproject.com/en/4.2/intro/reusable-apps/)
- [Two Scoops of Django](https://www.feldroy.com/books/two-scoops-of-django-3-x)

---

*总览文档由 Claude Code 生成*
*最后更新: 2026-02-15*
*版本: v2.0 - ✅ 所有阶段完成（全部视图重构完成，项目整体100%）*
