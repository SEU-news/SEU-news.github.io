# 阶段 5-10: 视图层重构计划

> **目标**: 重构所有视图模块，使用服务层
> **总时间**: 12.5 小时
> **依赖**: 阶段 1, 2, 3

---

## 📋 视图模块重构清单

### 阶段 5: 认证模块重构（1.5h）

**文件**: `api/views/auth.py`

**当前代码**: 186 行
**目标**: ~100 行

**重构要点**:
- 使用 `AuthService` 处理登录/注册/修改密码
- 使用 `PasswordHandler` 处理密码
- 统一异常处理

**重构前**:
```python
class LoginAPIView(APIView):
    def post(self, request):
        # 80+ 行业务逻辑...
```

**重构后**:
```python
class LoginAPIView(APIView):
    def post(self, request):
        try:
            user = AuthService.login(
                username=request.data.get('username'),
                password=request.data.get('password')
            )
            # ...
        except APIException as e:
            return Response({'success': False, 'message': e.message}, status=e.status)
```

---

### 阶段 6: 内容管理重构（2h）

**文件**: `api/views/content.py`

**当前代码**: 380 行
**目标**: ~180 行

**重构要点**:
- 使用 `ContentService` 处理内容操作
- 使用 `Content.contents` (Manager) 简化查询
- 统一异常处理

**代码对比**:
```python
# 重构前：
queryset = Content.objects.filter(
    status__in=['draft', 'pending', 'reviewed', 'rejected', 'published']
)

# 重构后：
queryset = Content.contents.active()
```

---

### 阶段 7: 发布管理重构（2.5h）

**文件**: `api/views/publish.py`

**当前代码**: 609 行
**目标**: ~250 行

**重构要点**:
- 使用 `PublishService` 处理发布逻辑
- 使用 `PDFService` 处理 PDF 生成
- 解决并发问题（时间戳命名）
- 使用 `Content.contents` 简化查询

---

### 阶段 8: 用户管理重构（1.5h）

**文件**: `api/views/admin.py`

**当前代码**: 238 行
**目标**: ~120 行

**重构要点**:
- 使用 `UserService` 处理用户管理
- 使用 `User_info.users` (Manager) 简化查询
- 统一异常处理

---

### 阶段 9: 文件处理重构（1h）

**文件**: `api/views/utility.py`

**当前代码**: 188 行
**目标**: ~100 行

**重构要点**:
- 使用 `FileService` 处理文件上传
- 统一异常处理

---

### 阶段 10: 视图层全面简化（2h）

**目标**: 统一视图模式，优化代码

**任务**:
1. 统一错误处理模式
2. 统一响应格式
3. 优化导入
4. 清理冗余代码

---

## ✅ 验证标准

### 各阶段验证标准

| 阶段 | 文件 | 当前行数 | 目标行数 | 验证要点 |
|------|------|----------|----------|----------|
| 5 | auth.py | 186 | ~100 | 登录/注册/修改密码正常 |
| 6 | content.py | 380 | ~180 | CRUD/审核/撤回/取消正常 |
| 7 | publish.py | 609 | ~250 | 发布/PDF生成正常 |
| 8 | admin.py | 238 | ~120 | 用户管理功能正常 |
| 9 | utility.py | 188 | ~100 | 文件上传功能正常 |
| 10 | 全部 | 1690 | ~750 | 所有端点正常 |

---

## 📝 执行步骤

1. **按顺序执行各阶段**
2. **每个阶段完成后运行测试**
3. **确保向后兼容**
4. **更新文档进度**

---

*视图层重构计划由 Claude Code 生成*
*最后更新: 2026-02-15*
